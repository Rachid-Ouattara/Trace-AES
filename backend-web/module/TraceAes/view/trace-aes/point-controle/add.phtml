<h1>Nouveau point de contrôle</h1>

<?php if ($erreurs): ?>
<div class="alert alert-danger">
    <ul class="mb-0">
        <?php foreach ($erreurs as $erreur): ?>
        <li><?= $this->escapeHtml($erreur) ?></li>
        <?php endforeach; ?>
    </ul>
</div>
<?php endif; ?>

<form method="post">
    <div class="mb-3">
        <label for="nom" class="form-label">Nom</label>
        <input type="text" name="nom" id="nom" class="form-control"
            value="<?= $this->escapeHtmlAttr($donnees['nom'] ?? '') ?>" required>
    </div>
    <div class="mb-3">
        <label for="type" class="form-label">Type</label>
        <select name="type" id="type" class="form-select" required>
            <option value="depot" <?= (isset($donnees['type']) && $donnees['type'] === 'depot') ? 'selected' : '' ?>>Dépôt</option>
            <option value="livraison" <?= (isset($donnees['type']) && $donnees['type'] === 'livraison') ? 'selected' : '' ?>>Livraison</option>
            <option value="checkpoint" <?= (isset($donnees['type']) && $donnees['type'] === 'checkpoint') ? 'selected' : '' ?>>Checkpoint</option>
        </select>
        <small class="form-text text-muted">Un point de contrôle de type "Dépôt" crée automatiquement le dépôt associé.</small>
    </div>
    <div class="mb-3">
        <label for="latitude" class="form-label">Latitude</label>
        <input type="number" step="0.000001" name="latitude" id="latitude" class="form-control"
            value="<?= $this->escapeHtmlAttr($donnees['latitude'] ?? '') ?>" required>
    </div>
    <div class="mb-3">
        <label for="longitude" class="form-label">Longitude</label>
        <input type="number" step="0.000001" name="longitude" id="longitude" class="form-control"
            value="<?= $this->escapeHtmlAttr($donnees['longitude'] ?? '') ?>" required>
    </div>
    <div class="mb-3">
        <label for="ville" class="form-label">Ville</label>
        <input type="text" name="ville" id="ville" class="form-control"
            value="<?= $this->escapeHtmlAttr($donnees['ville'] ?? '') ?>" required>
    </div>
    <div class="mb-3">
        <label for="pays" class="form-label">Pays</label>
        <input type="text" name="pays" id="pays" class="form-control"
            value="<?= $this->escapeHtmlAttr($donnees['pays'] ?? 'Burkina Faso') ?>" required>
    </div>
    <button type="submit" class="btn btn-success">Enregistrer</button>
</form>

<p class="mt-3"><a href="/trace-aes/point-controle">&larr; Retour à la liste</a></p>
