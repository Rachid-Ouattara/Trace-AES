<h1>Nouveau chargement</h1>

<?php if ($erreurs): ?>
<div class="alert alert-danger">
    <ul class="mb-0">
        <?php foreach ($erreurs as $erreur): ?>
        <li><?= $this->escapeHtml($erreur) ?></li>
        <?php endforeach; ?>
    </ul>
</div>
<?php endif; ?>

<form method="post">
    <div class="mb-3">
        <label for="citerne_id" class="form-label">Citerne</label>
        <select name="citerne_id" id="citerne_id" class="form-select" required>
            <option value="">-- Sélectionner --</option>
            <?php foreach ($citernes as $citerne): ?>
            <option value="<?= $this->escapeHtmlAttr($citerne['id']) ?>"
                <?= (isset($donnees['citerne_id']) && $donnees['citerne_id'] == $citerne['id']) ? 'selected' : '' ?>>
                <?= $this->escapeHtml($citerne['immatriculation']) ?>
            </option>
            <?php endforeach; ?>
        </select>
    </div>
    <div class="mb-3">
        <label for="depot_id" class="form-label">Dépôt</label>
        <select name="depot_id" id="depot_id" class="form-select" required>
            <option value="">-- Sélectionner --</option>
            <?php foreach ($depots as $depot): ?>
            <option value="<?= $this->escapeHtmlAttr($depot['id']) ?>"
                <?= (isset($donnees['depot_id']) && $donnees['depot_id'] == $depot['id']) ? 'selected' : '' ?>>
                <?= $this->escapeHtml($depot['nom']) ?>
            </option>
            <?php endforeach; ?>
        </select>
    </div>
    <div class="mb-3">
        <label for="agent_depot_id" class="form-label">Agent de dépôt</label>
        <select name="agent_depot_id" id="agent_depot_id" class="form-select" required>
            <option value="">-- Sélectionner --</option>
            <?php foreach ($agentsDepot as $agent): ?>
            <option value="<?= $this->escapeHtmlAttr($agent['id']) ?>"
                <?= (isset($donnees['agent_depot_id']) && $donnees['agent_depot_id'] == $agent['id']) ? 'selected' : '' ?>>
                <?= $this->escapeHtml($agent['prenom'] . ' ' . $agent['nom']) ?>
            </option>
            <?php endforeach; ?>
        </select>
    </div>
    <div class="mb-3">
        <label for="volume_declare_litres" class="form-label">Volume déclaré (L)</label>
        <input type="number" step="0.01" name="volume_declare_litres" id="volume_declare_litres"
            class="form-control" value="<?= $this->escapeHtmlAttr($donnees['volume_declare_litres'] ?? '') ?>" required>
    </div>
    <div class="mb-3">
        <label for="destination_id" class="form-label">Destination</label>
        <select name="destination_id" id="destination_id" class="form-select" required>
            <option value="">-- Sélectionner --</option>
            <?php foreach ($destinations as $destination): ?>
            <option value="<?= $this->escapeHtmlAttr($destination['id']) ?>"
                <?= (isset($donnees['destination_id']) && $donnees['destination_id'] == $destination['id']) ? 'selected' : '' ?>>
                <?= $this->escapeHtml($destination['nom']) ?> (<?= $this->escapeHtml($destination['ville']) ?>)
            </option>
            <?php endforeach; ?>
        </select>
    </div>
    <button type="submit" class="btn btn-success">Enregistrer le chargement</button>
</form>

<p class="mt-3"><a href="/trace-aes/chargement">&larr; Retour à la liste</a></p>
